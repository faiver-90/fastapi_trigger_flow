# import redis.asyncio as redis
# from redis.asyncio.client import Redis
#
# redis_client: Redis = redis.Redis(host="redis",
#                                   port=6379,
#                                   db=0,
#                                   decode_responses=True)
